import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beehive.cm2_app',
  appName: 'CM2_app',
  webDir: 'dist/cm-2/browser'
};

export default config;
